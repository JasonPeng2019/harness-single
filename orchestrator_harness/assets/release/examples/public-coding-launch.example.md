# Public coding launch asset

Use `orchestrator_harness.operator_launch` to launch the native
`orchestrator_harness.lane_controller` CLI and retain the exact PID plus
creation identity. The controller owns provider lifecycle, events, results,
resume, claims, and cleanup.
