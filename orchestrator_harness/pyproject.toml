[project]
name = "mcp-trial-orchestrator-harness"
version = "0.1.0"
description = "Read-only event watcher for MCP-Trial-3 parallel firmware lanes"
requires-python = ">=3.11"
dependencies = []

[tool.pyright]
pythonVersion = "3.11"
extraPaths = [".."]
reportImplicitRelativeImport = "none"
