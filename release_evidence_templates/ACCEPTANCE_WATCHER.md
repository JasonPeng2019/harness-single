# Acceptance watcher record

- Candidate coordinate (branch, commit, worktree root) and attempt ID:
- Watcher identity, model, effort, tier, PID-plus-creation identity, and timestamps:
- Read-only scope and `evaluator_enabled: false` proof:
- Ready, heartbeat, report, abort (if any), exit, and reap evidence:
- Caller/workflow role that requested the watcher and the decision owner:
