# Custom super-cache payloads

This directory is reserved for local, non-shipped custom payloads.  The
shipped product tree keeps it empty except for this marker; runtime staging
never reads it unless a local operator explicitly places a payload here.
