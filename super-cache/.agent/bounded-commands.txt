# The authoritative opt-in list of finite commands that must use run-bounded.
# One case-insensitive fragment per line. Keep them specific to avoid surprises.
# List tests, scripts, builds, or other finite commands; never list agent or
# subagent sessions (for example codex exec, claude -p, or launch-agent).
#
# Examples:
# npm run e2e
# docker compose up
